{
  "//": [
    "US-E1 · EAS Build/Submit profiles (Tech Spec §16.4). Three profiles, as the spec names",
    "them: development, preview, production.",
    "",
    "EXPO_PUBLIC_API_BASE is deliberately ABSENT from preview and production. app.config.ts",
    "throws when it is missing outside development, so a store build cannot be produced",
    "pointing at a developer's laptop. Supply it per environment with:",
    "  eas secret:create --scope project --name EXPO_PUBLIC_API_BASE --value https://api.kupkop.ph/api/v1",
    "",
    "⚠️ Nothing here can BUILD until the Apple Developer and Google Play accounts exist —",
    "the first of the five launch gates. The configuration lands now so that the day the",
    "credentials arrive is a build, not a sprint."
  ],
  "cli": {
    "version": ">= 12.0.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "//": "A debug binary with the dev client, for running on a device against a laptop.",
      "developmentClient": true,
      "distribution": "internal",
      "env": {
        "EXPO_PUBLIC_API_BASE": "http://localhost:8000/api/v1"
      }
    },
    "preview": {
      "//": [
        "Release-configuration binary, distributed internally — TestFlight/internal track.",
        "This is what the pre-launch smoke suite and the US-E3 rollback rehearsal run against."
      ],
      "distribution": "internal",
      "android": { "buildType": "apk" },
      "channel": "preview"
    },
    "production": {
      "//": "Store binary. autoIncrement keeps build numbers monotonic without hand-editing.",
      "autoIncrement": true,
      "channel": "production"
    }
  },
  "submit": {
    "production": {
      "//": [
        "EAS Submit credentials. Every value here is owner-supplied and none of them exist",
        "yet — see the pre-launch checklist. Left as explicit placeholders rather than",
        "omitted, so the shape of what is missing is visible.",
        "ios.appleId — the Apple ID that owns the app record",
        "ios.ascAppId — App Store Connect's numeric app id, created with the app record",
        "ios.appleTeamId — from the Apple Developer account",
        "android.serviceAccountKeyPath — the Play service-account JSON; NEVER commit it"
      ],
      "ios": {
        "appleId": "REPLACE_WITH_APPLE_ID",
        "ascAppId": "REPLACE_WITH_ASC_APP_ID",
        "appleTeamId": "REPLACE_WITH_TEAM_ID"
      },
      "android": {
        "serviceAccountKeyPath": "./play-service-account.json",
        "track": "internal"
      }
    }
  }
}
